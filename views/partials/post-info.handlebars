        <article class="post">
            <div class="title">
                <a href="/post/{{id}}">{{title}}</a>
                    <div class="meta">
                        Posted by <i class="fa-solid fa-circle-user"></i> {{user.username}} on {{format_date created_at}}
                    </div>
            </div>
            <div class="post-content">
                {{post_text}}
            </div>
        </article>